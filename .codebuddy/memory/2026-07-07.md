# 2026-07-07 工作日志

## 完成事项
- 与用户深入讨论了 Agent 桌面应用的架构设计
- 确定了技术栈：Tauri v2 + React + TypeScript + Rust
- 明确了迭代路线：V0.1 纯对话 → V0.2 工具系统 → V0.3 知识库 → V0.4 自动化 → V1.0 生态
- 从 skills-marketplace 安装了 7 个开发辅助 skills 到项目 `.codebuddy/skills/`
- 创建了项目 MEMORY.md 记录关键决策

## 后续进展
- Rust 环境已修复并验证：rustc 1.96.1, cargo 1.96.1, rustup 1.29.0
- 创建了 Tauri v2 + React 项目骨架 (`agent-desktop/`)
- V0.1 前端已搭建：对话窗口 + 侧边栏 + 设置页 + 流式输出 + Zustand 状态管理 + 明暗主题
- TypeScript 类型检查通过，Vite 构建通过

- 创建了 start-dev.bat / build.bat 启动脚本，修复了空格路径和编码问题
- 创建了 README.md 文档说明项目状态
- tauri dev 启动成功，桌面窗口正常显示

- 创建了 FastAPI 后端代理服务 (`agent-desktop/backend/`)，支持流式 SSE 转发
- 用户要求改为纯端侧架构，去掉 FastAPI，改为 Rust 端直接用 reqwest 调 LLM API
- Rust 端通过 Tauri invoke + event 机制实现流式对话：前端 invoke("chat_stream") → Rust 用 reqwest streaming → emit event("stream-token") 推送每个 token 到前端
- 启动脚本简化：不再需要外部 Python 服务，直接 npm run tauri dev 即可
- Cargo.toml 新增 reqwest (stream+json+rustls-tls), tokio, futures 依赖

- 实现了模型管理系统：支持多个 Provider（如 OpenAI、DeepSeek），每个 Provider 下管理多个模型
- 对话页顶部添加模型下拉选择器，类似 Cursor/Codex 的切换体验
- 设置页重构：AI 服务改为 Provider 卡片式管理，支持添加/编辑/删除 Provider，每个 Provider 可动态增删模型
- Store 重构：providers[] 替代旧的 apiKey/apiBase/model 单值，自动迁移旧数据
- ChatView 增加环境检测，非 Tauri 环境自动降级为 fetch 直连 LLM API

- 创建了 .gitignore，排除 node_modules/、dist/、target/、store.json、*.db、*.sqlite、data/、__pycache__/、.codebuddy/ 等敏感/构建产物
- Rust 端 setup 阶段自动创建应用数据目录（app_data_dir），确保首次启动时 store.json 的目录存在
- 用户下载后直接运行即可，无需手动创建任何配置目录
- v0.1.0 首次提交并推送到 GitHub（93 files, 15822 lines）：重写了 README.md、新建 CHANGELOG.md、根目录 README 简化
- CHANGELOG.md 遵循 Keep a Changelog 格式，记录 v0.1.0 所有功能

- Markdown 渲染已实现：使用 react-markdown + remark-gfm + react-syntax-highlighter (Prism + oneDark 主题)
  - 创建了 MarkdownRenderer.tsx 组件，支持标题/列表/引用/链接/表格/代码块
  - 代码块带语言标签 + 复制按钮，内联代码用等宽字体高亮
  - ChatView.tsx 中 AI 消息使用 MarkdownRenderer，用户消息保持纯文本
  - 新增 100+ 行 CSS 样式覆盖 markdown 所有元素

## 待办
- 快捷键支持
