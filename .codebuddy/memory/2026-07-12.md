# 2026-07-12

- **深度思考可视化 P0 已实现**：后端 `lib.rs` 新增 `ThinkingStart/ThinkingDelta/ThinkingStop` 事件，`run_completion()` 解析 `reasoning_content` 并发射事件；前端 `ChatView.tsx` 新增 `ThinkingBlock` 折叠面板组件，`appStore.ts` Message 接口新增 `thinking`/`thinkingStats` 字段
- **TODO**：深度思考模块需要解耦重构 —— 后端 `run_completion()` SSE 解析与 thinking 逻辑耦合，前端 ChatView 中 Tauri 事件监听和 fetch SSE 回调两套重复的思考处理代码
- **修复 code-server 7 个原生模块**：winregistry/windows_process_tree/windows(deviceid)/watchdog/spdlog/vscode-sqlite3/crypt32 全部通过 MSBuild 编译（根因：VS 2026 Insiders 未装 Spectre 缓解库，需 `/p:SpectreMitigation=false`）
- **修复 build.bat "系统找不到指定的文件" 错误**：echo 中的 `<--` 被 CMD 解析为输入重定向 `<`，需用 `^` 转义为 `^<--`
- **集成桌宠（Codex hatch-pet 风格）到 Agent-Desktop**：新增透明 alwaysOnTop 的 Tauri 第二窗口 `pet`（`tauri.conf.json` + `pet.html` + `vite.config.ts` 多入口）；前端 `src/pet/`（PetApp + spriteRenderer 程序化可爱宠物 + 兼容 Codex 8×9 精灵图）；Rust `pet.rs`（`toggle_pet`/`pet_interact`/`get_pet_stats` + 数值持久化到 app_data_dir/pet.json）；宠物直接监听已有事件 `thinking-start/tool-call/tool-result/stream-done/stream-error` 切换状态；侧边栏 PawIcon 开关。升级真实宠物：放 `public/pets/default/{pet.json,spritesheet.webp}`（1536×1872, 8×9, 192×208/帧）
- **环境坑**：本机 `cargo check` 触发 rustc-dev 工具链 ICE（`assertion failed: distance <= start` in rmeta/decoder.rs），是调试增量缓存损坏导致，与代码无关；用 `CARGO_INCREMENTAL=0` 全量重编可规避。前端 `tsc --noEmit` 已通过
